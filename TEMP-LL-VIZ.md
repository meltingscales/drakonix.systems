let's make the linkedlist vertically display, please. instead of horizontal.                                              

let's also add an "edit node" and "delete node" feature, with small icons, just like the "drag" feature. both new         
  features should also update the REPL.
